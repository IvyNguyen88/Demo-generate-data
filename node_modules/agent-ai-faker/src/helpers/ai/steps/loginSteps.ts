// import { Given, When, Then } from '@cucumber/cucumber';
// import { expect } from '@playwright/test';
// import { page } from './pageObjects'; // Adjust import as needed

// Given('a user is on the login page', async () => {
//   export const givenAUserIsOnTheLoginPage = async () => {
//   await page.goto('/login');
// };
// });
// When('the user enters valid username and password', async () => {
//   page.locator('input[name="username"]').fill('valid_username');
// page.locator('input[name="password"]').fill('valid_password');
// });
// When('the user clicks the login button', async () => {
//   await page.locator('button:has-text("Login")').click();
// });
// Then('the user should be redirected to the dashboard', async () => {
//   await expect(page).toHaveURL(/dashboard/);
// });